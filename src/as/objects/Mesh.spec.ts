// import {Mesh} from './Mesh'

describe('Mesh', (): void => {
	test('.Extending', (): void => {
		// expect(a.x).toBe(0)
	})

	test('.Instancing', (): void => {
		// expect(a.x).toBe(0)
	})

	test('.isMesh', (): void => {
		// expect(a.x).toBe(0)
	})

	test('.setDrawMode', (): void => {
		// expect(a.x).toBe(0)
	})

	test('.copy', (): void => {
		// expect(a.x).toBe(0)
	})

	test('.updateMorphTargets', (): void => {
		// expect(a.x).toBe(0)
	})

	test('.raycast', (): void => {
		// expect(a.x).toBe(0)
	})

	test('.clone', (): void => {
		// expect(a.x).toBe(0)
	})
})
