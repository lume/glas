import {Material} from './Material'

describe('Materials', () => {
	describe('Material', () => {
		todo('isMaterial')
		todo('onBeforeCompile')
		todo('setValues')
		todo('toJSON')
		todo('clone')
		todo('copy')
		todo('dispose')
	})
})
