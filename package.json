{
	"scripts": {
		"build:html": "mkdir -p build/ && cp -R src/index.html build/",
		"instrument": "rm -rf node_modules/three && npm i && node instrument.js && npm run build:html && serve .",
		"// ^ instrument": "Instruments Three.js code in node_modules, runs a simple demo, then the console shows which classes and their methods were used.",
		"instrument:reset": "rm -rf node_modules/three && npm i"
	},
	"devDependencies": {
		"@types/node": "^12.0.2",
		"at-at": "^1.0.0",
		"serve": "^11.3.2",
		"three": "^0.125.0",
		"typescript": "^3.4.5"
	},
	"dependencies": {}
}
